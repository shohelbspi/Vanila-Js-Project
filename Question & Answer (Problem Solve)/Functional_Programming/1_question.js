// What is a pure function, and why is it useful in UI rendering?

// Answer :: A pure function always returns the same output for the same input and has no side effects. In UI rendering (such as React), pure functions make components predictable, easier to test, easier to debug, and more efficient to render.